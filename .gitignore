# Ignorowanie katalogu node_modules (zainstalowane zależności)
node_modules/

# Ignorowanie plików logów
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorowanie plików konfiguracyjnych środowiska (jeśli dodasz w przyszłości)
.env

# Ignorowanie plików systemowych
.DS_Store
Thumbs.db

# Ignorowanie plików cache
.cache/
tmp/

# Ignorowanie plików lock innego menedżera pakietów (opcjonalne)
package-lock.json